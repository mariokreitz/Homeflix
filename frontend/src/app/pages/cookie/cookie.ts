import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
    selector: 'app-cookie',
    templateUrl: './cookie.html',
    styleUrl: './cookie.css',
    imports: [ RouterLink ],
    standalone: true,
})
export class Cookie {

}